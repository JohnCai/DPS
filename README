1. cut the CSV from FTP folder to 'source' folder
2. run main.py to iterate CSVs in 'source' folder and convert them to XML and drop them in 'target/Date' folder. orign CSVs would be archived in 'source_archive' folder
3. copy the XMLs to target FTP folders.